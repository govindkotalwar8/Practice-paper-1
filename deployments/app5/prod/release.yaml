apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: app5
  namespace: app5
spec:
  chart:
    spec:
      chart: app5-chart
      version: 0.1.0
      sourceRef:
        kind: GitRepository
        name: app5-repo
        namespace: flux-system
  values:
    image:
      repository: nginx
      tag: 1.28.2
